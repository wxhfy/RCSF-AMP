# Submission result summaries (2026-08-04)

Compact result summaries associated with the current SUCF manuscript revision.
The files are copied from the local experiment outputs and contain no model
checkpoints or predictions.

- `aggregate_results.csv`, `per_seed_results.csv`: same-split 2026 baselines.
- `pepnet_batch128.csv`, `sucf_full_batch128.*`: efficiency comparison.
- `ssfgm_*`: SSFGM reproduction metrics for both benchmarks.
- `plddt_controls_*`: fixed-checkpoint pLDDT control protocol and metrics.
